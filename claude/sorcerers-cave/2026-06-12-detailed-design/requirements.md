Write a detailed requirements/design specification for the VAX Macro-32 implementation of the game *Sorcerer's Cave*. This design specification should be all that Claude AI would need to re-implement the game in a different language. 

As such you don't need to concentrate implementation details, but the high-level design and then detailed requirements to implement each feature of the game.

You should weave in the original game rules to this document,
clarifying any ambiguity in the original rules for solitaire play based on the detail available in the implementation.

Present the design specification as a standalone HTML document. Use any diagrams that might help explain the design, but always be mindful that it is Claude Code that is the primary audience.

The final goal should allow it to be re-implemented in a different language, and possible in a web-browser rather than a terminal, so the display requirements should be logical units rather than column/row accurate.

Also include anything that isn't currently implemented in
the VAX Macro-32 version that is a gap between the original
manual and the implementation - including as much detail
as is required to fully specify the feature.

Write the specification into a file in this directory.